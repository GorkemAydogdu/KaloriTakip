@model KaloriTakip.Models.Class.Class1
@{
    ViewBag.Title = "Anasayfa";
}
<title>Anasayfa</title>
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/fontawesome/6.1.0/css/all.min.css" integrity="sha512-
10/jx2EXwxxWqCLX/hHth/vu2KY3jCF70dCQB8TSgNjbCVAC/8vai53GfMDrO2Emgwccf
2pJqxct9ehpzG+MTw==" crossorigin="anonymous" referrerpolicy="no-referrer" />

<style>
    .iconify {
        color: gold;
    }

    .card-header:last-child { /*card headerın son elemanının border bottom kaldır*/
        border-bottom: none;
    }
</style>
@using (Html.BeginForm("Anasayfa", "Kullanici", FormMethod.Post))
{
    <input type="date" class="form-control my-2" name="tarih" />
    <button class="btn btn-primary">Ara</button>
}
<div class="card my-2">
    <div class="card-header">
        <span class="fw-bold fs-3">
            <span class="iconify" data-icon="emojione-v1:sunrise" data-width="32" data-height="32"></span>
            Breakfast
        </span>
        <span class="fw-bold fs-4 float-end">
            @Session["KahvaltiKalori"].ToString()
        </span>
    </div>
    <div class="card-body">
        <p class="card-title fs-4 d-flex justify-content-between fw-bold">
            @foreach (var m in Model.kahvaltı) /* Model içerisinde bulunan kahvaltı kadar dön ve m elemanınn yemek tablosundaki adını yaz */
            {
                @m.tbl_Yemek.YemekAdi;
                <br />
            }
            <span class="text-end">
                @foreach (var m in Model.kahvaltı) /* Model içerisinde bulunan kahvaltı kadar dön ve m elemanınn kalorisini yaz */
                {
                    @m.Kalori;
                    <a class="btn btn-danger" href="/Kullanici/Sil/@m.ID">Sil</a>
                    <br />
                }
            </span>
        </p>
    </div>
</div>
<div class="card my-4">
    <div class="card-header">
        <span class="fw-bold fs-3">
            <span class="iconify" data-icon="emojione:sun" data-width="32" data-height="32"></span>
            Lunch
        </span>
        <span class="fw-bold fs-4 float-end">
            @Session["OgleKalori"].ToString()
        </span>
    </div>
    <div class="card-body">
        <p class="card-title fs-4 d-flex justify-content-between fw-bold">
            @foreach (var m in Model.ogle) /* Model içerisinde bulunan ogle kadar dön ve m elemanınn yemek tablosundaki adını yaz */
            {
                @m.tbl_Yemek.YemekAdi;
                <br />
            }
            <span class="text-end">
                @foreach (var m in Model.ogle) /* Model içerisinde bulunan ogle kadar dön ve m elemanınn kalorisini yaz */
                {
                    @m.Kalori;
                    <a class="btn btn-danger" href="/Kullanici/Sil/@m.ID">Sil</a>
                    <br />
                }
            </span>
        </p>
    </div>
</div>
<div class="card my-4">
    <div class="card-header">
        <span class="fw-bold fs-3">
            <span class="iconify" data-icon="noto:sunset" data-width="32" data-height="32"></span>
            Dinner
        </span>
        <span class="fw-bold fs-4 float-end">
            @Session["AksamKalori"].ToString()
        </span>
    </div>
    <div class="card-body">
        <p class="card-title fs-4 d-flex justify-content-between fw-bold">
            @foreach (var m in Model.aksam) /* Model içerisinde bulunan aksam kadar dön ve m elemanınn yemek tablosundaki adını yaz */
            {
                @m.tbl_Yemek.YemekAdi;
                <br />
            }
            <span class="text-end">
                @foreach (var m in Model.aksam) /* Model içerisinde bulunan aksam kadar dön ve m elemanınn kalorisini yaz */
                {
                    @m.Kalori;
                    <a class="btn btn-danger" href="/Kullanici/Sil/@m.ID">Sil</a>
                    <br />
                }
            </span>
        </p>
    </div>
</div>
<div class="card my-4">
    <div class="card-header">
        <span class="fw-bold fs-3">
            <span class="iconify" data-icon="noto:night-with-stars" data-width="32" data-height="32" data-flip="horizontal"></span>
            Snacks/Other
        </span>
        <span class="fw-bold fs-4 float-end">
            @Session["AraOgunKalori"].ToString()
        </span>
    </div>
    <div class="card-body">
        <p class="card-title fs-4 d-flex justify-content-between fw-bold">
            @foreach (var m in Model.ara) /* Model içerisinde bulunan ara kadar dön ve m elemanınn yemek tablosundaki adını yaz */
            {
                @m.tbl_Yemek.YemekAdi;
                <br />
            }
            <span class="text-end">
                @foreach (var m in Model.ara) /* Model içerisinde bulunan ara kadar dön ve m elemanınn kalorisini yaz */
                {
                    @m.Kalori;
                    <a class="btn btn-danger" href="/Kullanici/Sil/@m.ID">Sil</a>
                    <br />
                }
            </span>
        </p>
    </div>
</div>
<div class="card my-4">
    <div class="card-header">
        <span class="fw-bold fs-3">
            Calories Consumed / Calories Remaining
        </span>
        <span class="float-end fw-bold fs-3">
            @Session["ToplamKalori"] / @Session["GunlukKalori"]
        </span>
    </div>
</div>

<script src="https://code.iconify.design/2/2.2.0/iconify.min.js"></script>
<script src="~/Scripts/jquery-3.6.0.min.js"></script>
<script src="~/Scripts/bootstrap.min.js"></script>
<script src="~/Content/JS/JavaScript.js"></script>